profile.env=dev
#  log config
log.home=/opt/logs
log.name=dms-webapp
log.level=DEBUG
log.only_sql=true

##  jdbc.properties configuration
dataSource.url=jdbc:mysql://localhost:3306/dms_dev?useUnicode=true&amp;characterEncoding=utf8
dataSource.username=root
dataSource.password=rootroot

wx.domain=http://localhost:8700

wx.scanning=http://localhost:8700/bind/auth?url=/wx/scanning&param={ORDER_NO}

wx.receipt=http://localhost:8700/bind/auth?url=/wx/receipt&param={ORDER_NO}